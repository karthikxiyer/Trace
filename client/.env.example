# Leave empty in development — Vite proxy handles /api → localhost:3001
# Set to your Render URL in production
VITE_API_URL=

# Signal bot number shown on login page (optional)
VITE_SIGNAL_BOT_NUMBER=
